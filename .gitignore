# Sensitive files
.env
auth.json
auth.lock

# Runtime state
state.db
state.db-shm
state.db-wal
gateway.pid
gateway_state.json
interrupt_debug.log
.update_check
.hermes_history

# Caches and logs
cache/
audio_cache/
image_cache/
logs/
models_dev_cache.json

# macOS metadata
.DS_Store

# .env backups (may contain secrets)
.env.bak.*
.env.*.bak

# Cron runtime output (keep cron/jobs.json tracked)
cron/output/

# Sessions and checkpoints
sessions/
checkpoints/

# Snapshots
.skills_prompt_snapshot.json

# Runtime channel directory (updates every session)
channel_directory.json

# Personal pastes
pastes/

# Embedded repos
hermes-agent/

# Lock files
cron/.tick.lock
memories/*.lock

# Binaries
bin/
